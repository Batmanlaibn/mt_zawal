<!DOCTYPE html>
<html lang="mn">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>SafeSchool — Дээрэлхэлтийн Мэдэгдлийн Систем</title>
  <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800;900&display=swap" rel="stylesheet" />
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    :root {
      --navy:    #04091f;
      --deep:    #080f2e;
      --blue:    #2563eb;
      --indigo:  #4f46e5;
      --violet:  #7c3aed;
      --sky:     #38bdf8;
      --mint:    #34d399;
      --white:   #ffffff;
      --muted:   rgba(180,200,255,0.65);
      --glass:   rgba(255,255,255,0.06);
      --border:  rgba(255,255,255,0.10);
    }

    html { scroll-behavior: smooth; }

    body {
      font-family: 'Plus Jakarta Sans', sans-serif;
      background: var(--navy);
      color: var(--white);
      min-height: 100vh;
      overflow-x: hidden;
    }

    /* ═══════════════════════════════════════
       BACKGROUND CANVAS
    ═══════════════════════════════════════ */
    .scene {
      position: fixed; inset: 0; z-index: 0;
      background:
        radial-gradient(ellipse 100% 70% at 15% 0%,   #1e3a8a40 0%, transparent 55%),
        radial-gradient(ellipse 80%  60% at 85% 90%,  #6d28d950 0%, transparent 55%),
        radial-gradient(ellipse 60%  50% at 50% 40%,  #0f172a 0%, transparent 80%),
        linear-gradient(170deg, #04091f 0%, #060d28 60%, #0c0620 100%);
    }

    /* Grid mesh */
    .grid-lines {
      position: fixed; inset: 0; z-index: 0; pointer-events: none;
      background-image:
        linear-gradient(rgba(99,102,241,0.07) 1px, transparent 1px),
        linear-gradient(90deg, rgba(99,102,241,0.07) 1px, transparent 1px);
      background-size: 60px 60px;
      mask-image: radial-gradient(ellipse 80% 80% at 50% 50%, black 30%, transparent 80%);
    }

    /* Orbs */
    .orb {
      position: fixed; border-radius: 50%;
      filter: blur(90px); pointer-events: none; z-index: 0;
      animation: orb-float 14s ease-in-out infinite alternate;
    }
    .orb-a { width:500px;height:500px; background:#1d4ed8; opacity:.18; top:-100px; left:-80px; animation-delay:0s; }
    .orb-b { width:600px;height:600px; background:#6d28d9; opacity:.15; bottom:-150px; right:-100px; animation-delay:-5s; }
    .orb-c { width:280px;height:280px; background:#0ea5e9; opacity:.12; top:35%; left:52%; animation-delay:-9s; }
    @keyframes orb-float {
      from { transform: translate(0,0) scale(1); }
      to   { transform: translate(28px,38px) scale(1.06); }
    }

    /* Stars */
    #starfield { position:fixed; inset:0; z-index:0; pointer-events:none; }

    /* ═══════════════════════════════════════
       NAVIGATION
    ═══════════════════════════════════════ */
    nav {
      position: fixed; top: 0; left: 0; right: 0; z-index: 100;
      display: flex; align-items: center; justify-content: space-between;
      padding: 0 clamp(1.5rem, 5vw, 3.5rem);
      height: 68px;
      background: rgba(4,9,31,0.55);
      backdrop-filter: blur(18px);
      border-bottom: 1px solid var(--border);
      opacity: 0;
      animation: slideDown 0.7s ease 0.1s forwards;
    }
    @keyframes slideDown {
      from { opacity:0; transform:translateY(-16px); }
      to   { opacity:1; transform:translateY(0); }
    }

    .nav-logo {
      display: flex; align-items: center; gap: 10px;
      text-decoration: none;
    }
    .logo-mark {
      width: 34px; height: 34px; border-radius: 9px;
      background: linear-gradient(135deg, #2563eb, #7c3aed);
      display: flex; align-items: center; justify-content: center;
      font-size: 1rem;
      box-shadow: 0 0 16px rgba(99,102,241,0.4);
    }
    .logo-text {
      font-weight: 800; font-size: 1rem; color: #fff; letter-spacing: -0.01em;
    }
    .logo-text span { color: #818cf8; }

    .nav-links {
      display: flex; align-items: center; gap: 0.2rem;
      list-style: none;
    }
    .nav-links a {
      display: block; padding: 0.45rem 0.9rem;
      font-size: 0.85rem; font-weight: 600;
      color: var(--muted); text-decoration: none;
      border-radius: 8px;
      transition: color 0.2s, background 0.2s;
    }
    .nav-links a:hover { color: #fff; background: var(--glass); }

    .nav-cta {
      display: flex; align-items: center; gap: 0.75rem;
    }
    .nav-btn {
      padding: 0.5rem 1.2rem; border-radius: 100px;
      font-family: inherit; font-size: 0.85rem; font-weight: 700;
      cursor: pointer; border: none; outline: none;
      transition: transform 0.2s, box-shadow 0.2s, background 0.2s;
    }
    .nav-btn-ghost {
      background: transparent; color: var(--muted);
      border: 1.5px solid var(--border);
    }
    .nav-btn-ghost:hover { color:#fff; border-color:rgba(255,255,255,0.3); background:var(--glass); }
    .nav-btn-solid {
      background: linear-gradient(135deg,#3b82f6,#7c3aed);
      color: #fff;
      box-shadow: 0 3px 16px rgba(99,102,241,0.45);
    }
    .nav-btn-solid:hover { transform:translateY(-2px); box-shadow:0 8px 28px rgba(99,102,241,0.55); }

    /* Hamburger */
    .nav-hamburger {
      display: none; flex-direction: column; gap: 5px; cursor:pointer; padding:4px;
    }
    .nav-hamburger span {
      display:block; width:22px; height:2px; background:#aab;
      border-radius:2px; transition:all 0.25s;
    }

    /* Mobile nav */
    .nav-mobile {
      display: none; position:fixed; top:68px; left:0; right:0;
      background: rgba(6,11,36,0.97); backdrop-filter:blur(20px);
      border-bottom:1px solid var(--border);
      padding: 1.2rem 1.5rem 1.5rem;
      flex-direction: column; gap: 0.4rem;
      z-index: 99;
    }
    .nav-mobile.open { display:flex; }
    .nav-mobile a {
      display:block; padding:0.65rem 0.8rem;
      color:var(--muted); text-decoration:none; font-weight:600; font-size:0.9rem;
      border-radius:8px; transition: color 0.2s, background 0.2s;
    }
    .nav-mobile a:hover { color:#fff; background:var(--glass); }
    .nav-mobile-btns { display:flex; gap:0.75rem; margin-top:0.8rem; flex-wrap:wrap; }
    .nav-mobile-btns .nav-btn { flex:1; min-width:120px; text-align:center; }

    /* ── Dropdown ── */
    .nav-links li { position: relative; }

    /* chevron on nav link */
    .nav-links > li > a .chev {
      display: inline-block; margin-left: 3px; font-size: 0.6rem;
      transition: transform 0.22s; vertical-align: middle; opacity: 0.6;
    }
    .nav-links > li.open > a .chev { transform: rotate(180deg); opacity: 1; }

    .dropdown {
      position: absolute; top: calc(100% + 14px); left: 50%;
      transform: translateX(-50%) translateY(10px);
      background: rgba(6,10,40,0.98);
      backdrop-filter: blur(28px);
      border: 1px solid rgba(99,102,241,0.25);
      border-radius: 18px;
      padding: 0.6rem;
      min-width: 240px;
      box-shadow: 0 28px 70px rgba(0,0,0,0.65), 0 0 0 1px rgba(255,255,255,0.04), 0 0 40px rgba(79,70,229,0.08);
      opacity: 0; pointer-events: none;
      transition: opacity 0.22s cubic-bezier(.22,.8,.38,1), transform 0.22s cubic-bezier(.22,.8,.38,1);
      z-index: 200;
    }
    /* arrow tip */
    .dropdown::before {
      content: '';
      position: absolute; top: -6px; left: 50%; transform: translateX(-50%);
      width: 12px; height: 12px;
      background: rgba(6,10,40,0.98);
      border-left: 1px solid rgba(99,102,241,0.25);
      border-top: 1px solid rgba(99,102,241,0.25);
      rotate: 45deg;
    }

    /* OPEN state — toggled by JS */
    .nav-links li.open .dropdown {
      opacity: 1; pointer-events: all;
      transform: translateX(-50%) translateY(0);
    }
    .nav-links li.open > a { color: #fff; background: var(--glass); }

    .drop-header {
      font-size: 0.66rem; font-weight: 800; letter-spacing: 0.13em;
      text-transform: uppercase; color: rgba(130,150,255,0.5);
      padding: 0.45rem 0.8rem 0.35rem;
    }
    .drop-item {
      display: flex; align-items: center; gap: 11px;
      padding: 0.62rem 0.8rem; border-radius: 11px;
      text-decoration: none; color: rgba(190,205,255,0.85);
      font-size: 0.84rem; font-weight: 600;
      transition: background 0.18s, color 0.18s, transform 0.18s;
      cursor: pointer;
    }
    .drop-item:hover { background: rgba(99,102,241,0.16); color: #fff; transform: translateX(2px); }
    .drop-icon {
      width: 32px; height: 32px; border-radius: 9px; flex-shrink: 0;
      display: flex; align-items: center; justify-content: center;
      font-size: 0.95rem;
    }
    .drop-icon.blue   { background: rgba(59,130,246,0.18); }
    .drop-icon.purple { background: rgba(124,58,237,0.18); }
    .drop-icon.green  { background: rgba(52,211,153,0.15); }
    .drop-icon.orange { background: rgba(251,146,60,0.15); }
    .drop-icon.pink   { background: rgba(232,121,249,0.15); }
    .drop-icon.sky    { background: rgba(56,189,248,0.15); }

    .drop-text { display: flex; flex-direction: column; gap: 1px; }
    .drop-text strong { font-size: 0.84rem; font-weight: 700; color: #e0e7ff; }
    .drop-text span   { font-size: 0.71rem; font-weight: 500; color: rgba(150,170,220,0.58); }

    .drop-divider {
      height: 1px; background: rgba(255,255,255,0.06); margin: 0.4rem 0.5rem;
    }

    @media (max-width: 768px) {
      .nav-links, .nav-cta { display: none; }
      .nav-hamburger { display: flex; }
    }

    /* ═══════════════════════════════════════
       HERO
    ═══════════════════════════════════════ */
    .hero {
      position: relative; z-index: 1;
      min-height: 100vh;
      display: flex; align-items: center; justify-content: center;
      padding: 68px 1.5rem 2rem;
    }

    .hero-inner {
      max-width: 820px; width: 100%;
      display: flex; flex-direction: column;
      align-items: center; text-align: center;
      gap: 0;
    }

    /* Status pill */
    .status-pill {
      display: inline-flex; align-items: center; gap: 8px;
      background: rgba(255,255,255,0.07);
      border: 1px solid rgba(99,102,241,0.35);
      border-radius: 100px;
      padding: 5px 16px 5px 10px;
      font-size: 0.72rem; font-weight: 700;
      letter-spacing: 0.08em; text-transform: uppercase;
      color: #a5b4fc;
      margin-bottom: 2rem;
      opacity: 0;
      animation: rise 0.8s ease 0.6s forwards;
    }
    .status-dot {
      width: 7px; height: 7px; border-radius: 50%;
      background: #34d399;
      box-shadow: 0 0 0 3px rgba(52,211,153,0.25);
      animation: pulse-dot 2.5s ease infinite;
      flex-shrink: 0;
    }
    @keyframes pulse-dot {
      0%,100% { box-shadow:0 0 0 3px rgba(52,211,153,0.25); }
      50%      { box-shadow:0 0 0 6px rgba(52,211,153,0.10); }
    }

    /* Main heading */
    .hero-heading {
      font-size: clamp(2.2rem, 6vw, 4.5rem);
      font-weight: 900;
      line-height: 1.08;
      letter-spacing: -0.03em;
      color: #fff;
      margin-bottom: 1.4rem;
      opacity: 0;
      animation: rise 0.9s ease 0.8s forwards;
    }
    .hero-heading .accent {
      background: linear-gradient(90deg, #60a5fa 0%, #a78bfa 50%, #e879f9 100%);
      -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text;
    }
    .hero-heading .line2 { display: block; }

    /* Sub */
    .hero-sub {
      font-size: clamp(1rem, 2vw, 1.2rem);
      font-weight: 500;
      color: var(--muted);
      line-height: 1.6;
      max-width: 560px;
      margin-bottom: 2.4rem;
      opacity: 0;
      animation: rise 0.9s ease 1s forwards;
    }
    .hero-sub strong { color: rgba(200,215,255,0.9); font-weight: 700; }

    /* Tags row */
    .tags {
      display: flex; flex-wrap: wrap; justify-content: center; gap: 0.6rem;
      margin-bottom: 2.4rem;
      opacity: 0;
      animation: rise 0.8s ease 1.15s forwards;
    }
    .tag {
      display: inline-flex; align-items: center; gap: 6px;
      background: rgba(99,102,241,0.12);
      border: 1px solid rgba(99,102,241,0.28);
      border-radius: 100px;
      padding: 5px 14px;
      font-size: 0.78rem; font-weight: 600;
      color: #c7d2fe;
    }
    .tag-dot { width:6px;height:6px;border-radius:50%;flex-shrink:0; }
    .tag-dot.green  { background:#34d399; }
    .tag-dot.blue   { background:#60a5fa; }
    .tag-dot.purple { background:#c084fc; }

    /* Buttons */
    .hero-btns {
      display: flex; flex-wrap: wrap; justify-content: center; gap: 1rem;
      margin-bottom: 3.5rem;
      opacity: 0;
      animation: rise 0.9s ease 1.3s forwards;
    }
    .btn {
      position: relative; overflow: hidden;
      display: inline-flex; align-items: center; gap: 10px;
      padding: 0.9rem 2rem; border-radius: 100px;
      font-family: inherit; font-size: 0.95rem; font-weight: 700;
      cursor: pointer; border: none; outline: none;
      transition: transform 0.22s, box-shadow 0.22s;
      white-space: nowrap;
    }
    .btn::after {
      content:''; position:absolute; inset:0;
      background:linear-gradient(105deg, transparent 30%, rgba(255,255,255,0.2) 50%, transparent 70%);
      transform:translateX(-120%);
      transition:transform 0.5s ease;
    }
    .btn:hover::after { transform:translateX(120%); }

    .btn-primary {
      background: linear-gradient(135deg,#2563eb 0%,#7c3aed 100%);
      color: #fff;
      box-shadow: 0 4px 24px rgba(79,70,229,0.5), inset 0 1px 0 rgba(255,255,255,0.15);
    }
    .btn-primary:hover { transform:translateY(-3px) scale(1.02); box-shadow:0 12px 36px rgba(79,70,229,0.6); }
    .btn-primary:active { transform:scale(0.97); }

    .btn-outline {
      background: rgba(255,255,255,0.06);
      color: #e0e7ff;
      border: 1.5px solid rgba(255,255,255,0.18);
      backdrop-filter: blur(8px);
    }
    .btn-outline:hover { transform:translateY(-3px) scale(1.02); background:rgba(255,255,255,0.12); border-color:rgba(255,255,255,0.32); }
    .btn-outline:active { transform:scale(0.97); }

    .btn-icon { font-size:1.1em; }

    /* Stats row */
    .stats {
      display: flex; flex-wrap: wrap; justify-content: center;
      gap: 0; border-radius: 16px;
      background: rgba(255,255,255,0.04);
      border: 1px solid var(--border);
      backdrop-filter: blur(12px);
      overflow: hidden;
      opacity: 0;
      animation: rise 0.9s ease 1.5s forwards;
      width: 100%; max-width: 600px;
    }
    .stat {
      flex: 1; min-width: 130px;
      display: flex; flex-direction: column; align-items: center;
      padding: 1.2rem 1rem;
      border-right: 1px solid var(--border);
      transition: background 0.2s;
    }
    .stat:last-child { border-right: none; }
    .stat:hover { background: rgba(255,255,255,0.04); }
    .stat-val {
      font-size: 1.6rem; font-weight: 900;
      background: linear-gradient(135deg,#60a5fa,#c084fc);
      -webkit-background-clip: text; -webkit-text-fill-color:transparent; background-clip:text;
      letter-spacing: -0.02em;
    }
    .stat-lbl {
      font-size: 0.73rem; font-weight: 600;
      color: var(--muted); margin-top: 2px;
      text-align: center;
    }

    /* Scroll hint */
    .scroll-hint {
      position: absolute; bottom: 2rem; left: 50%; transform: translateX(-50%);
      display: flex; flex-direction: column; align-items: center; gap: 6px;
      opacity: 0;
      animation: rise 0.8s ease 2s forwards;
      cursor: pointer;
    }
    .scroll-hint span { font-size:0.7rem; font-weight:600; color:rgba(150,170,220,0.5); letter-spacing:0.1em; text-transform:uppercase; }
    .scroll-arrow {
      width:22px; height:36px; border:2px solid rgba(150,170,220,0.25);
      border-radius:12px; position:relative;
    }
    .scroll-arrow::before {
      content:''; position:absolute; width:4px;height:8px;
      background:rgba(150,170,220,0.5); border-radius:2px;
      top:5px; left:50%; transform:translateX(-50%);
      animation: scroll-bob 2s ease infinite;
    }
    @keyframes scroll-bob {
      0%,100% { opacity:1; top:5px; }
      50%      { opacity:0; top:14px; }
    }

    @keyframes rise {
      from { opacity:0; transform:translateY(28px); }
      to   { opacity:1; transform:translateY(0); }
    }

    /* ═══════════════════════════════════════
       RESPONSIVE
    ═══════════════════════════════════════ */
    @media (max-width: 520px) {
      .hero-heading { font-size: 1.9rem; letter-spacing:-0.02em; }
      .hero-btns { flex-direction:column; align-items:center; }
      .btn { width:100%; max-width:300px; justify-content:center; }
      .stats { flex-direction:column; }
      .stat { border-right:none; border-bottom:1px solid var(--border); }
      .stat:last-child { border-bottom:none; }
    }
  </style>
</head>
<body>

<!-- Background -->
<div class="scene"></div>
<div class="grid-lines"></div>
<div class="orb orb-a"></div>
<div class="orb orb-b"></div>
<div class="orb orb-c"></div>
<canvas id="starfield"></canvas>

<!-- ── NAVIGATION ── -->
<nav id="nav">
  <a href="#" class="nav-logo">
    <div class="logo-mark">🛡️</div>
    <span class="logo-text">Safe<span>School</span></span>
  </a>

  <ul class="nav-links">

    <!-- Нүүр -->
    <li>
      <a href="#">Нүүр <span class="chev">▼</span></a>
      <div class="dropdown">
        <div class="drop-header">Хурдан хандах</div>
        <a class="drop-item" href="nuur.html">
          <div class="drop-icon blue">🏠</div>
          <div class="drop-text"><strong>Нүүр хуудас</strong><span>Системийн тухай танилцуулга</span></div>
        </a>
        <a class="drop-item" href="nuur.html">
          <div class="drop-icon purple">📊</div>
          <div class="drop-text"><strong>Статистик</strong><span>Мэдэгдлийн тоо, дүн шинжилгээ</span></div>
        </a>
        <a class="drop-item" href="nuur.html">
          <div class="drop-icon green">📰</div>
          <div class="drop-text"><strong>Мэдээ, мэдээлэл</strong><span>Сүүлийн үеийн шинэчлэлүүд</span></div>
        </a>
      </div>
    </li>

    <!-- Мэдэгдэл өгөх -->
    <li>
      <a href="#">Мэдэгдэл өгөх <span class="chev">▼</span></a>
      <div class="dropdown">
        <div class="drop-header">Мэдэгдлийн төрөл</div>
        <a class="drop-item" href="index.html">
          <div class="drop-icon blue">📢</div>
          <div class="drop-text"><strong>Шинэ мэдэгдэл</strong><span>Дээрэлхэлт мэдэгдэх</span></div>
        </a>
        <a class="drop-item" href="index.html">
          <div class="drop-icon orange">🔍</div>
          <div class="drop-text"><strong>Мэдэгдлийн төлөв</strong><span>Илгээсэн мэдэгдлээ шалгах</span></div>
        </a>
        <div class="drop-divider"></div>
        <a class="drop-item" href="index.html">
          <div class="drop-icon purple">🔒</div>
          <div class="drop-text"><strong>Нэргүй мэдэгдэл</strong><span>Нууцлалтайгаар мэдэгдэх</span></div>
        </a>
      </div>
    </li>

    <!-- Асуулга -->
    <li>
      <a href="#">Асуулга <span class="chev">▼</span></a>
      <div class="dropdown">
        <div class="drop-header">Асуулгын төрөл</div>
        <a class="drop-item" href="aguulga.html">
          <div class="drop-icon green">📋</div>
          <div class="drop-text"><strong>Суралцагчийн асуулга</strong><span>Сурагчдад зориулсан</span></div>
        </a>
        <a class="drop-item" href="aguulga.html">
          <div class="drop-icon sky">👩‍🏫</div>
          <div class="drop-text"><strong>Багшийн асуулга</strong><span>Багш, ажилтнуудад</span></div>
        </a>
        <a class="drop-item" href="aguulga.html">
          <div class="drop-icon pink">👨‍👩‍👧</div>
          <div class="drop-text"><strong>Эцэг эхийн асуулга</strong><span>Гэр бүлд зориулсан</span></div>
        </a>
      </div>
    </li>

    <!-- Тусламж -->
    <li>
      <a href="#">Тусламж <span class="chev">▼</span></a>
      <div class="dropdown">
        <div class="drop-header">Дэмжлэг & Мэдээлэл</div>
        <a class="drop-item" href="#">
          <div class="drop-icon blue">❓</div>
          <div class="drop-text"><strong>Түгээмэл асуулт</strong><span>Хариултыг эндээс олоорой</span></div>
        </a>
        <a class="drop-item" href="#">
          <div class="drop-icon green">🤝</div>
          <div class="drop-text"><strong>Зөвлөгөө авах</strong><span>Мэргэжилтэнтэй холбогдох</span></div>
        </a>
        <a class="drop-item" href="#">
          <div class="drop-icon orange">📚</div>
          <div class="drop-text"><strong>Гарын авлага</strong><span>Хэрэгтэй материалууд</span></div>
        </a>
        <div class="drop-divider"></div>
        <a class="drop-item" href="#">
          <div class="drop-icon purple">🆘</div>
          <div class="drop-text"><strong>Яаралтай тусламж</strong><span>Шуурхай холбоо барих</span></div>
        </a>
      </div>
    </li>

    <!-- Холбоо барих -->
    <li>
      <a href="#">Холбоо барих <span class="chev">▼</span></a>
      <div class="dropdown">
        <div class="drop-header">Бидэнтэй холбогдох</div>
        <a class="drop-item" href="holboo_barih.html">
          <div class="drop-icon sky">✉️</div>
          <div class="drop-text"><strong>И-мэйл</strong><span>info@safeschool.mn</span></div>
        </a>
        <a class="drop-item" href="holboo_barih.html">
          <div class="drop-icon green">📞</div>
          <div class="drop-text"><strong>Утас</strong><span>1800-1234 (үнэгүй)</span></div>
        </a>
        <a class="drop-item" href="holboo_barih.html">
          <div class="drop-icon blue">💬</div>
          <div class="drop-text"><strong>Онлайн чат</strong><span>Бодит цагийн дэмжлэг</span></div>
        </a>
      </div>
    </li>

  </ul>

  <div class="nav-cta">
    <button class="nav-btn nav-btn-ghost"><a href="newtreh.html">Нэвтрэх</a></button>
    <button class="nav-btn nav-btn-solid"><a href="index.html">Мэдэгдэл өгөх</a></button>
  </div>

  <div class="nav-hamburger" id="hamburger" aria-label="Цэс">
    <span></span><span></span><span></span>
  </div>
</nav>

<!-- Mobile nav drawer -->
<div class="nav-mobile" id="mobileNav">
  <a href="#">🏠 &nbsp;Нүүр</a>
  <a href="#">📢 &nbsp;Мэдэгдэл өгөх</a>
  <a href="#">📋 &nbsp;Асуулга</a>
  <a href="#">🤝 &nbsp;Тусламж</a>
  <a href="#">📞 &nbsp;Холбоо барих</a>
  <div class="nav-mobile-btns">
    <button class="nav-btn nav-btn-ghost">Нэвтрэх</button>
    <button class="nav-btn nav-btn-solid">Мэдэгдэл өгөх</button>
  </div>
</div>

<!-- ── HERO ── -->
<section class="hero">
  <div class="hero-inner">

    <div class="status-pill">
      <span class="status-dot"></span>
      Систем идэвхтэй — 24/7 хяналттай
    </div>

    <h1 class="hero-heading">
      Сургуулийн <span class="accent">Дээрэлхэлтийн</span>
      <span class="line2">Мэдэгдлийн Систем</span>
    </h1>

    <p class="hero-sub">
      Хэрэв чи эсвэл найз чинь дээрэлхэлтэнд өртсөн бол —
      <strong>нэргүйгээр, аюулгүйгээр</strong> мэдэгдэж болно.
      Бид чамтай хамт байна.
    </p>

    <div class="tags">
      <span class="tag"><span class="tag-dot green"></span>Нууцлал хамгаалагдсан</span>
      <span class="tag"><span class="tag-dot blue"></span>Нэргүй мэдэгдэл</span>
      <span class="tag"><span class="tag-dot purple"></span>Шуурхай хариу үйлдэл</span>
    </div>

    <div class="hero-btns">
      <button class="btn btn-primary">
        <span class="btn-icon">📢</span>
        Мэдэгдэл өгөх
      </button>
      <button class="btn btn-outline">
        <span class="btn-icon">📋</span>
        Асуулга бөглөх
      </button>
    </div>

    <div class="stats">
      <div class="stat">
        <span class="stat-val">100%</span>
        <span class="stat-lbl">Нууцлалтай</span>
      </div>
      <div class="stat">
        <span class="stat-val">&lt;24h</span>
        <span class="stat-lbl">Хариу хугацаа</span>
      </div>
      <div class="stat">
        <span class="stat-val">0</span>
        <span class="stat-lbl">Нэр шаардахгүй</span>
      </div>
      <div class="stat">
        <span class="stat-val">24/7</span>
        <span class="stat-lbl">Онлайн дэмжлэг</span>
      </div>
    </div>

  </div>

  <div class="scroll-hint">
    <div class="scroll-arrow"></div>
    <span>Доош</span>
  </div>
</section>

<script>
  /* ── Star canvas ── */
  const canvas = document.getElementById('starfield');
  const ctx = canvas.getContext('2d');
  let stars = [];

  function resize() {
    canvas.width  = window.innerWidth;
    canvas.height = window.innerHeight;
  }
  resize();
  window.addEventListener('resize', () => { resize(); buildStars(); });

  function buildStars() {
    stars = Array.from({ length: 140 }, () => ({
      x: Math.random() * canvas.width,
      y: Math.random() * canvas.height,
      r: Math.random() * 1.4 + 0.3,
      a: Math.random(),
      speed: 0.004 + Math.random() * 0.008,
      phase: Math.random() * Math.PI * 2,
    }));
  }
  buildStars();

  function drawStars(t) {
    ctx.clearRect(0, 0, canvas.width, canvas.height);
    stars.forEach(s => {
      const alpha = 0.15 + 0.55 * (0.5 + 0.5 * Math.sin(t * s.speed + s.phase));
      ctx.beginPath();
      ctx.arc(s.x, s.y, s.r, 0, Math.PI * 2);
      ctx.fillStyle = `rgba(200,215,255,${alpha})`;
      ctx.fill();
    });
    requestAnimationFrame(drawStars);
  }
  requestAnimationFrame(drawStars);

  /* ── Dropdown — click to open/close ── */
  const navItems = document.querySelectorAll('.nav-links > li');
  navItems.forEach(li => {
    const link = li.querySelector('a');
    const drop = li.querySelector('.dropdown');
    if (!drop) return;
    link.addEventListener('click', e => {
      e.preventDefault();
      const isOpen = li.classList.contains('open');
      // close all
      navItems.forEach(x => x.classList.remove('open'));
      if (!isOpen) li.classList.add('open');
    });
  });
  // click outside to close
  document.addEventListener('click', e => {
    if (!e.target.closest('.nav-links')) {
      navItems.forEach(x => x.classList.remove('open'));
    }
  });

  /* ── Hamburger ── */
  const hbg = document.getElementById('hamburger');
  const mob = document.getElementById('mobileNav');
  hbg.addEventListener('click', () => {
    mob.classList.toggle('open');
    const bars = hbg.querySelectorAll('span');
    if (mob.classList.contains('open')) {
      bars[0].style.transform = 'translateY(7px) rotate(45deg)';
      bars[1].style.opacity = '0';
      bars[2].style.transform = 'translateY(-7px) rotate(-45deg)';
    } else {
      bars.forEach(b => { b.style.transform=''; b.style.opacity=''; });
    }
  });

  /* ── Button ripple ── */
  document.querySelectorAll('.btn, .nav-btn').forEach(btn => {
    btn.addEventListener('click', function(e) {
      const rect = this.getBoundingClientRect();
      const size = Math.max(rect.width, rect.height) * 2.2;
      const el = document.createElement('span');
      el.style.cssText = `
        position:absolute; border-radius:50%; pointer-events:none;
        width:${size}px; height:${size}px;
        left:${e.clientX - rect.left - size/2}px;
        top:${e.clientY - rect.top - size/2}px;
        background:rgba(255,255,255,0.15);
        transform:scale(0); animation:ripple 0.55s ease-out forwards;
      `;
      this.appendChild(el);
      setTimeout(() => el.remove(), 600);
    });
  });
  const style = document.createElement('style');
  style.textContent = `@keyframes ripple{to{transform:scale(1);opacity:0;}}`;
  document.head.appendChild(style);

  /* ── Nav scroll tint ── */
  window.addEventListener('scroll', () => {
    const nav = document.getElementById('nav');
    nav.style.background = window.scrollY > 30
      ? 'rgba(4,9,31,0.85)'
      : 'rgba(4,9,31,0.55)';
  });
</script>
</body>
</html>